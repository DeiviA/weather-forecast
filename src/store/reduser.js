import * as actionsType from './actions';

const initialState = {
    currentCity: 'kyiv',
    cities: []
}

const reducer = (state = initialState, action) => {
    
}

export default reducer;